# Variable fonts are large binaries — store in git-lfs (the root .gitattributes,
# generated by the flake, covers images/audio/data but not fonts).
*.ttf filter=lfs diff=lfs merge=lfs -text
*.otf filter=lfs diff=lfs merge=lfs -text
*.woff2 filter=lfs diff=lfs merge=lfs -text
