// The 404 / 403 / 500 pages now live in `@evinvest/uikit` (NotFound / Forbidden /
// ServerError) and are rendered straight from the app's route files. Coming-soon
// stays app-local — it's a landing-specific embeddable surface.
export { ComingSoonView } from "./ui/coming-soon";
